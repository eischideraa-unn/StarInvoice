{
  "name": "starinvoice-sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK wrapper for the StarInvoice Soroban contract",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest",
    "lint": "echo \"No linter configured\""
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.6.0",
    "soroban-client": "^1.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.0.0"
  }
}
